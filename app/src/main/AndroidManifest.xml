<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ryuu.attendanceapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AntiSkipClass"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.mydomain.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.authentication.LoginActivity" />
        <activity android:name=".activities.authentication.SignUpActivity" />
        <activity android:name=".activities.ClassActivity" />
        <activity android:name=".activities.ClassDetailsActivity" />
        <activity android:name=".activities.CreateCourseActivity" />
        <activity android:name=".activities.CourseDetails" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".activities.profile.activity_myprofile" />
        <activity android:name=".activities.profile.activity_edit_profile" />
        <activity android:name=".activities.ClassList_Teacher_Activity" />
        <activity android:name=".activities.ClassListDetails" />
        <activity android:name=".activities.AnswerActivity">
            android:windowSoftInputMode="stateHidden|adjustPan"
        </activity>
        <activity android:name=".activities.authentication.Reset_Password_Activity" />
    </application>

</manifest>